import {StyleSheet} from "react-native"

const Styles = StyleSheet.create({
    Text:{
        fontSize:15,
        color:"#fff",
        marginTop:10,
        marginBottom:15,
        fontWeight:"bold"
    },
    flatlistStyle:{
        marginBottom:15
    }
})

export default Styles